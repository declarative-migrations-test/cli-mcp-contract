[package]
org = "declarative-migrations-test"
name = "cli-mcp-contract"
version = "0.1.0"
description = "Independent mcp acceptance harness"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/declarative-migrations-test/cli-mcp-contract"

[install]
dir = ".vendor/.zed"

[publish]
exclude = ["artifacts/**", "vendor/**", ".vendor/**", ".env", ".env.*"]
